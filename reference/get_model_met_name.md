# get the name of model meteo file

get the name of the meteo file from the model config file

## Usage

``` r
get_model_met_name(model, config_f)
```

## Arguments

- model:

  Model name

- config_f:

  path to model specific controll file
