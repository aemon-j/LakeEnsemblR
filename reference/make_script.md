# Creates a script to then run as a job

Creates a script to then run as a job

## Usage

``` r
make_script(call, name)
```

## Arguments

- call:

  an object of class \`call“

- name:

  name of the job
